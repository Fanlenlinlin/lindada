<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#ececec</item>
        <item name="colorPrimaryDark">#a3a3a3</item>
        <item name="colorAccent">@color/purple</item>
        <item name="toolbarStyle">@style/ClubToolbar</item>
        <!--自定义AlertDialog-->
        <item name="alertDialogTheme">@style/MySelfAlertDialog</item>
    </style>

    <style name="LoadActivity2Theme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#d4d4d4</item>
        <item name="colorPrimaryDark">#d4d4d4</item>
        <item name="colorAccent">@color/purple</item>
        <item name="toolbarStyle">@style/ClubToolbar</item>
        <!--自定义AlertDialog-->
        <item name="alertDialogTheme">@style/MySelfAlertDialog</item>
        <!-- 为 Theme 设置背景图 -->
        <item name="android:windowBackground">@drawable/loadding_first</item>
    </style>

    <style name="ZhiBoTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#ececec</item>
        <item name="colorPrimaryDark">#000</item>
        <item name="colorAccent">@color/purple</item>
        <item name="toolbarStyle">@style/ClubToolbar</item>
        <!--自定义AlertDialog-->
        <item name="alertDialogTheme">@style/MySelfAlertDialog</item>
    </style>

    <!--自定义AlertDialog-->
    <style name="MySelfAlertDialog" parent="@style/Theme.AppCompat.Light.Dialog.Alert">
        <!--修改AlertDialog按钮的颜色-->
        <item name="colorAccent">@color/purple</item>
    </style>

    <style name="ClubToolbar" parent="Widget.AppCompat.Toolbar">
        <item name="contentInsetStart">0dp</item><!-- 设置该属性解决空白部分-->
    </style>

    <style name="mypopwindow_anim_style">
        <item name="android:windowEnterAnimation">@anim/popshow_anim</item>
        <!-- 指定显示的动画xml -->
        <item name="android:windowExitAnimation">@anim/pophidden_anim</item>
        <!-- 指定消失的动画xml -->
    </style>

    <style name="NobackDialog" parent="@android:style/Theme.Dialog">
        　<!--更换背景图片实现全透明-->
        <item name="android:windowBackground">@android:color/transparent</item>
        　<!--屏幕背景不变暗-->　
        <item name="android:backgroundDimEnabled">false</item>
    </style>


    <style name="city_name_popwindow_anim_style">
        <item name="android:windowEnterAnimation">@anim/city_name_popshow_anim</item>
        <!-- 指定显示的动画xml -->
        <item name="android:windowExitAnimation">@anim/city_name_pophidden_anim</item>
        <!-- 指定消失的动画xml -->
    </style>

    <!--直播聊天室字体风格,加阴影-->
    <style name="ChatRoom_TextView">
        <item name="android:shadowColor">#40000000</item>
        <item name="android:shadowDx">3</item>
        <item name="android:shadowDy">3</item>
        <item name="android:shadowRadius">3</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--下载APK进度条-->
    <style name="ProgressBar_Mini" parent="@android:style/Widget.ProgressBar.Horizontal">
        <item name="android:maxHeight">50dip</item>
        <item name="android:minHeight">8dip</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_indeterminate_horizontal</item>
        <item name="android:progressDrawable">@drawable/progress_mini</item>
    </style>
</resources>
